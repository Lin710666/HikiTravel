# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
backend/.venv/

# ---- 本机配置：高德 Key、模型地址等，不要提交 ----
.env
backend/.env
# Qwen TTS WebUI 的安装位置（一行绝对路径，比如 C:\Users\xxx\Desktop\qwen-tts-webui）。
# 那是**这台机器**的路径，别人 clone 下来没用，而且会泄露本机目录结构。
qwen-tts-home.txt

# ---- 运行期数据：SQLite 计划库、角色卡、缓存等，都是本机状态 ----
backend/data/
# 音轨库与热点词表落在**项目根**的 data/（见 /api/audio 返回的 dir）。
#
# ⚠ 前面那个 `/` 不能省。gitignore 里不带前导斜杠的 `data/` 会匹配
# **任意层级**叫 data 的目录 —— 写成 `data/` 会把 `backend/app/data/`
# （当前项目自己的词表、能力清单、提示词）也一起忽略掉，
# 于是那些文件根本不会被提交，别人 clone 下来就是缺文件的。
# 加前导 `/` 才是"只锚定仓库根目录下的 data/"。
#
# v6.6 起曾**不再忽略** data/videos 与 data/audio（当时的需求是"演示用的宣传片
# 与配乐一起发到仓库，clone 即有素材"）。
#
# ★ 但推 HikiTravel 仓库时**没有带它们**：HikiTravel-AIRI-1.1 那个分支里根本没有
#   data/ 目录。所以这里恢复成忽略整个 data/，和已发布的 1.1 保持一致，原因两条：
#     1. 体积：data/videos 126 MB + data/audio 22 MB ≈ 148 MB；
#     2. 授权：借物表.md 第 2 节写明那些视频"未取得授权、请勿用于商业用途或再分发"，
#        而"随仓库分发"本身就是再分发。
#   素材仍然留在本机，本机演示一切照旧；别人 clone 下来开屏会走程序化的西湖场景、
#   配乐列表为空 ——「检查环境.bat」会把这一点清楚地提示出来。
#
# 要让素材一起进仓库（自包含）：把下面那行注释掉即可。
/data/

# ---- Node / 前端 ----
node_modules/
frontend/dist/
frontend/dist-embed/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# ---- 本地自用素材（授权不允许再配布，所以永不提交）----
# 洛天依模型：作者配布规则禁止二次配布 / 禁止商用，仅本地演示使用。
public/models3d/luotianyi/

# 深闺藏衣袖（Live2D）：模之屋作品 https://www.aplaybox.com/details/model/hy42BmpCDhzT
# 原作者 云梦泽yumoze（年年子）。发布页写明：
#   禁止商用 / 禁止二次配布 / 禁止修改模型 / 禁止用于制作视频之外的用途
# 所以和洛天依一样只留本机演示，**不进仓库**。
# 用 tools\装模型.ps1 -Live2D ... 装进来的，同一套规则。
public/models/cangyixiu/

# 唯有牡丹真国色（Live2D）：模之屋作品 https://www.aplaybox.com/details/model/Fax9M3k0LVMP
# 原作者同样是 云梦泽yumoze（年年子），规则与「深闺藏衣袖」一致：
#   禁止商用 / 禁止二次配布 / 禁止修改模型 / 禁止用于制作视频之外的用途
# 只留本机演示，**不进仓库**。
public/models/mudan/
